/**
 * Ruta /mis-mascotas/[id]/caso. Layout compartido de la ruta (estructura y providers).
 */
import "@/estilos/chat.css";
import "@/estilos/coordinacion.css";

export default function LayoutCoordinacion({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
