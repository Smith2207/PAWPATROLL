/**
 * Ruta /chats. Layout compartido de la ruta (estructura y providers).
 */
import "@/estilos/chat.css";
import "@/estilos/coordinacion.css";

export default function LayoutChats({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
